{
  "name": "idleon-tool",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add -A && git commit -m 'deploy' && git push origin main && git push gitee main"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
